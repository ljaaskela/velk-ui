#include "cube_visual.h"

namespace velk::ui {

// No out-of-line methods today — cube is a pure RT-only geometry marker.
// File exists so the class has a translation unit for future raster
// mesh rendering.

} // namespace velk::ui
